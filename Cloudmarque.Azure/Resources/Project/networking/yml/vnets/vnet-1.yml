vnetName: vnet-1

location: UK South

dnsServers: []                  # (Optional) Array of dns servers

vnetpeerings: []                # (Optional) Array of vnet names to peer with.

addressSpace: []                # (Optional) Array of vnet cidr blocks

service:
  publish:
    vnet:

subnets:
  - cidr:
    subnetName:
    routeTable:                 # Optional
    networkSecurityGroup:       # Optional

