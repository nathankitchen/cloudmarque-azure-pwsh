vnetName: vnet-1

location: UK South

service:
  publish:
    vnet:

addressSpace:                  # Array of vnet cidr blocks
  -

subnets:
  - cidr:
    subnetName:
    routeTable:                 # Optional
    networkSecurityGroup:       # Optional

