# you can provide any combination of routes or routegroups.
# group files must exist in ./groups folder relative to file they are called from.

- tableName:
  location: # Optional - defaults to resource group location
  service:
    publish:
      routeTable:
  routes:
    - cidr:
      nextHopType:
      routeName:
      nextHopIpAddress:

- tableName:
  service:
    publish:
      routeTable:
  routeGroup:
    - DefaultRouteGroup
  routes:
    - cidr:
      nextHopType:
      routeName:
      nextHopIpAddress:
