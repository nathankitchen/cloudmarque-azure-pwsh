resourceGroupServiceTag:             # For deployment to existing resource group | cm-service tag value for new resource group
resourceGroupName:                   # For creating new resource groups
location:                            # Shared Image gallery region

galleryName:                         # Shared Image gallery name

imageDefinitions:
  - definitionName:
    publisher:
    offer:
    sku:
    location:                        # Optional
    osType:                          # Optional
    osState:                         # Optional
    endOfLifeDate:                   # Optional
    versions:
      - version:
        imageServiceTag:
        endOfLifeDate:               # Optional
        replicaCount:                # Optional
        targetRegions:
